name: panel
description: "The web administration panel for the FF Alarm environment."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  fluent_ui: 4.8.7
  shared_preferences: 2.2.3
  quickalert: 1.1.0
  dio: 5.4.3+1
  archive: 3.4.10

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
